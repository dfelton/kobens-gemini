#!/bin/bash

#
# Trade History Processes
#
ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history aaveusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history aaveusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history ampusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history ampusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history balusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history balusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history batusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history batusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history bchusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history bchusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history btcusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history btcusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history compusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history compusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history crvusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history crvusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history ethusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history ethusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history filusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history filusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history kncusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history kncusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history linkusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history linkusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history ltcusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history ltcusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history manausd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history manausd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history mkrusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history mkrusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history oxtusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history oxtusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history renusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history renusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history snxusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history snxusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history storjusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history storjusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history umausd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history umausd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history uniusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history uniusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history yfiusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history yfiusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history zecusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history zecusd &
    sleep 0.2
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history zrxusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini logger:trade-history zrxusd &
    sleep 0.2
fi
