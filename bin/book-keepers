#!/bin/bash

#
# Book Keepers
#
ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper 1inchusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper 1inchusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper aaveusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper aaveusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper ampusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper ampusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper balusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper balusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper batusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper batusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper bchusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper bchusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper bntusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper bntusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper btcusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper btcusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper compusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper compusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper crvusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper crvusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper ethusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper ethusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper enjusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper enjusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper filusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper filusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper kncusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper kncusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper linkusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper linkusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper lrcusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper lrcusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper ltcusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper ltcusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper manausd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper manausd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper mkrusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper mkrusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper oxtusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper oxtusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper renusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper renusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper sandusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper sandusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper snxusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper snxusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper sklusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper sklusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper storjusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper storjusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper umausd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper umausd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper uniusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper uniusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper yfiusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper yfiusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper zecusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper zecusd &
    sleep 0.25
fi

ps aux | grep -v grep | grep -q "$(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper zrxusd"
if [ $? -eq 1 ]; then
    php -n $(realpath $(dirname $BASH_SOURCE))/gemini market:book-keeper zrxusd &
    sleep 0.25
fi
