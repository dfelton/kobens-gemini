
--
-- Example Permissions
--

-- Table Permissions
--   GRANT SELECT,UPDATE,INSERT ON gemini.* TO gemini@localhost

-- Database Permissions
--   GRANT LOCK TABLES ON gemini.* TO gemini@localhost

-- Throttler Permissons
--   GRANT SELECT ON gemini_throttler.throttler TO gemini_throttler@localhost
--   GRANT UPDATE (count, time) ON gemini_throttler.throttler TO gemini_throttler@localhost

